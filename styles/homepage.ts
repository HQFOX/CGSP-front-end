import { css } from '@emotion/css';

export const styles = {
	main: css({
		minheight: '100vh',
		backgroundColor: 'white'
	}),
	logoContainer: css({
		display: 'flex',
		justifyContent: 'center',
		paddingTop: '40px'
	}),
	mapContainer: css({
		height: 480
	})
};
